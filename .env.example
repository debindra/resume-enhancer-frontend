# Frontend Environment Configuration

# API Configuration
NEXT_PUBLIC_API_URL=http://localhost:8000/api/v1

# Note: API keys should NEVER be exposed in frontend
# All LLM calls must go through the backend API
